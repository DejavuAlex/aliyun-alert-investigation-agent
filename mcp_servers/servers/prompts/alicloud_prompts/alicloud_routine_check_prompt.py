from mcp.types import PromptMessage, TextContent

from mcp_servers.base_server import BaseServer
from mcp_servers.servers.prompts.prompt_mixin import PromptMixin

"""
日常安全监控
"""
class ALICLOUD_ROUTING_CHECK_PROMPT(BaseServer,PromptMixin):
    def __init__(self,cmd_config):
        super().__init__(cmd_config,"alicloud_routing_check_Server", prefix="alicloud_routing_check_prompt")
        self.setup_server()

    def setup_server(self):
        @self.mcp_instance.tool
        def routing_sas_check(
                access_key_id: str,
                region_id: str
        ):
            """
            对阿里云安全中心一段时间以来的网络安全事件告警的归纳总结分析prompt，找出安全威胁
            """
            FUNCTION = f"""
                        你是一个云安全智能体,通过分析阿里云sas发现黑客入侵的蛛丝马迹。
                        """

            AUDIENCE = """
                        - 云安全工程师
                        """
            STEPS = f"""
            1. 攻击源与威胁情报分析

            AI应推理： “这些攻击源IP是否属于已知的僵尸网络或攻击者集群？它们是否在协同攻击？”
            
            检查点：
            
            统计高频攻击源IP、端口和地区。
            
            识别攻击源IP的行为模式（如：扫描、暴力破解、漏洞利用）。
            
            关联同一攻击源对不同资产发起的攻击，判断其攻击意图和范围。
            
            2. 受害资产与暴露面分析
            
            AI应推理： “为什么总是这几台ECS实例被攻击？它们是否存在共同的脆弱性？”
            
            检查点：
            
            识别被高频攻击的资产（ECS、SLB、RDS等）。
            
            分析受害资产是否存在共同特征（如：同一安全组、暴露相同端口、位于同一VSwitch）。
            
            评估资产的暴露程度（是否不必要的暴露在公网）。
            
            3. 攻击手法与漏洞利用分析
            
            AI应推理： “攻击者尝试利用CVE-2023-XXXXX漏洞，但我们的系统是否真的存在此漏洞？是否需要紧急修补？”
            
            检查点：
            
            识别主要的攻击类型（如：Web漏洞利用、暴力破解、DDoS攻击、恶意软件通信）。
            
            分析攻击载荷中涉及的特定CVE编号或漏洞利用手法。
            
            判断这些攻击手法是否针对了你环境中真实存在的服务版本。
            
            4. 安全策略有效性评估
            
            AI应推理： “安全组虽然阻挡了大部分攻击，但为什么仍有成功入侵的案例？是不是某条规则过于宽松？”
            
            检查点：
            
            评估当前安全组、网络ACL和WAF策略的有效性。
            
            检查是否存在“放行过多”的规则（如：0.0.0.0/0对管理端口）。
            
            分析告警事件中的“已拦截”与“已爆发”事件比例，评估防御体系的整体效能。
            
            5. 横向移动与内部威胁迹象
            
            AI应推理： “这台失陷主机是否正在内部网络中进行扫描或攻击其他服务器？”
            
            检查点：
            
            寻找内部IP地址之间可疑连接的模式。
            
            检测来自内部网络的、针对数据库或管理端口的攻击尝试。
            
            识别可能表明主机已失陷的迹象（如：对外连接至已知C2服务器、内部端口扫描）。
        """
            TEMPLATE = f"""
            ### 阿里云安全中心网络安全隐患分析报告

            **分析时间范围**: [例如：2024年X月X日 - 2024年X月X日]
            **报告生成时间**: [当前日期]
            **执行摘要**:
            [用2-3句话概括最严重的安全隐患和整体安全态势。例如：“过去两月内发现来自IP段X.X.X.X的持续性暴力破解攻击，主要目标为暴露公网的ECS，且部分安全组规则存在过度授权，潜在数据泄露风险高。”]
            
            ---
            
            #### **详细隐患分析**
            
            **隐患 1: [概括性标题，如‘面向公网的Redis服务暴露及暴力破解’]**
            
            - **风险等级**: 🔴 高危 / 🟡 中危 / 🟢 低危
            - **涉及资产**: [例如：ECS(i-xxxxxx), 安全组(sg-xxxxxx)]
            - **关联事件示例**:
                - `[时间]` - `[事件类型]` - `[源IP]` -> `[目标资产:端口]` (e.g., `2024-05-20 14:30:01` - `暴力破解攻击` - `103.1.2.3` -> `ECS(i-123abc):6379`)
                - `[时间]` - `[事件类型]` - `[源IP]` -> `[目标资产:端口]`
            - **隐患描述与AI推理**:
                [**核心部分**：详细解释此隐患的根源、被利用的可能性以及潜在影响。例如：“分析发现，安全组`sg-xxxxxx`对0.0.0.0/0开放了Redis默认端口6379，这直接导致该服务暴露在公网。过去两月内共捕获XXX次针对此端口的暴力破解攻击。若攻击成功，可能导致数据被窃取、篡改或服务器被植入挖矿木马。”]
            - **修复与加固建议**:
                - **立即行动**: [例如：1. 修改安全组`sg-xxxxxx`，禁止公网IP对6379端口的访问。 2. 检查Redis服务是否使用弱密码，立即修改。]
                - **长期优化**: [例如：1. 将Redis迁移至私有子网。 2. 通过跳板机进行访问。]
            
            **隐患 2: [例如：'利用[特定漏洞]的攻击尝试']**
            - **风险等级**: ...
            - ... (结构与隐患1相同)
            
            ---
            
            #### **整体安全态势与总结建议**
            
            1.  **攻击趋势分析**:
                -   最频繁的攻击类型：[例如：SSH暴力破解]
                -   最主要的威胁来源：[例如：IP段 X.X.X.X]
                -   最脆弱的资产类型：[例如：配置了公网IP的ECS]
            
            2.  **优先级行动清单**:
                -   **[P0]**: [最高优先级的1-2项修复任务，如：立即收紧XX安全组规则]。
                -   **[P1]**: [次优先级的任务，如：为所有Linux服务器安装云安全中心客户端]。
                -   **[P2]**: [长期改进任务，如：全面梳理网络架构，收敛公网暴露面]。
            
            3.  **监控与预警建议**:
                -   建议对[特定IP段]的访问设置更严格的告警阈值。
                -   推荐启用安全中心的[例如：防暴力破解、 webshell检测] 等高级功能。
            
            **备注**: 本报告基于阿里云安全中心在指定时间段内已识别的网络攻击事件。对于已绕过防护的成功入侵，需结合主机日志和流量日志进行更深度的取证分析。
            """
            prompt = f"""       
                                {FUNCTION.strip()}
                                目标受众：
                                {AUDIENCE.strip()}
                                   
                                请按以下思维步骤（Chain-of-Thought）逐步推理：
                                {STEPS.strip()}
                                输出必须严格遵循以下模板：
                                {TEMPLATE.strip()}
                                强制规则：
                                {self.mandatory_rules.strip()}
                                约束条件：
                                {self.constraints.strip()}
                                                """

            return PromptMessage(
                role="assistant",
                content=TextContent(
                    type="text",
                    text=prompt)
            )
        @self.mcp_instance.tool
        def routing_actiontrail_check(
                access_key_id: str,
                region_id: str
        ):
            """
            对阿里云审计事件actiontrail在一段时间的归纳总结分析的prompt，找出安全威胁，
            """
            FUNCTION = f"""
                        你是一个云安全智能体,通过分析阿里云action trail发现黑客入侵的蛛丝马迹。
                        """

            AUDIENCE = """
                        - 云安全工程师
                        """
            STEPS = f"""
            请遵循以下框架，在每一步中主动进行逻辑推理：

            1. 身份与访问管理异常
            
            AI应推理： “这些登录行为是否构成了一个异常模式？这个IP地址虽然不在威胁情报库中，但其行为（例如，在root登录后立即出现）是否可疑？”
            
            检查点：
            
            异常时间登录（非工作时间、与历史模式不符）。
            
            陌生地理位置和IP地址（包括TOR、VPN、高风险地区）。
            
            敏感的权限提升操作（如CreateAccessKey, AttachPolicyToUser, UpdateAccessKey）。
            
            凭证泄露迹象（同一AK从地理上不可能的两个IP发起请求）。
            
            2. 资源操作异常
            
            AI应推理： “这一系列资源操作（创建ECS -> 修改安全组 -> 创建快照）是否构成了一个完整的攻击链？其目的是什么？”
            
            检查点：
            
            突然创建资源（ECS、RDS），特别是配置异常（如开放所有端口的安全组）。
            
            修改关键网络配置（安全组、网络ACL、路由表）。
            
            数据泄露迹象（大量GetObject、DescribeInstances、CreateSnapshot操作）。
            
            持久化与后门行为（创建未被授权的定时任务、修改系统镜像）。
            
            3. API调用行为模式异常
            
            AI应推理： “这个用户通常每天调用10次API，今天却调用了1000次。这是否是攻击者在进行地毯式侦察？”
            
            检查点：
            
            高频API调用（远超正常基线）。
            
            “地毯式”侦察行为（连续调用大量Describe..., List... API）。
            
            攻击行为序列（识别典型的攻击链，如：登录 -> 列举资源 -> 提升权限 -> 窃取数据）。
            
            4. 安全配置变更风险
            
            AI应推理： “这个安全组规则的修改，虽然只开放了一个端口，但结合后续的外网IP绑定操作，其风险等级就变得非常高了。”
            
            检查点：
            
            安全组、网络ACL策略被修改得更宽松。
            
            路由表被添加了指向未知设备或公网的路由。
            
            密钥策略或权限策略被降级。
                        """
            TEMPLATE = f"""
            ### 阿里云 ActionTrail 安全分析报告

            **分析时间范围**: [输入日志的起始时间] 至 [输入日志的结束时间]
            **执行摘要**:
            [用2-3句话概括发现的最关键风险，例如：“发现来自IP X.X.X.X的未授权用户‘test-user’在非工作时间创建了高权限RAM策略并试图枚举所有OSS存储桶，置信度高。”]

            ---

            #### **详细可疑事件列表**

            **事件 1: [简短的事件标题，如‘可疑的地理位置登录与权限提升’]**

            - **严重等级**: 🔴 高 / 🟡 中 / 🟢 低
            - **发生时间**: [UTC时间]
            - **主要用户/角色**: [RAM用户名、角色ARN或AccessKey ID]
            - **源IP地址**: [IP地址] (地理位置: [国家/城市])
            - **AI推理与判断**:
                [**这是关键部分**：详细阐述你为什么认为这些事件是可疑的。将离散的事件串联起来，解释它们之间的逻辑关系，并推断攻击者的可能意图。例如：“尽管单个的`ConsoleSignin`事件可能正常，但其发生在美国深夜，且紧随其后的是该用户调用了`CreateAccessKey`，这与正常运维行为模式不符，高度怀疑是攻击者在获取持久化访问凭证。”]
            - **关键事件序列**:
                1.  `[时间]` - `[事件名称]` (e.g., `ConsoleSignin`)
                2.  `[时间]` - `[事件名称]` (e.g., `CreateAccessKey`)
                3.  `[时间]` - `[事件名称]` (e.g., `DescribeInstances`)
            - **调查建议/下一步行动**:
                - [ ] 立即禁用用户 `[用户名]` 及其相关的AccessKey `[AccessKey ID]`。
                - [ ] 检查安全组 `[安全组ID]` 的规则变更并回滚。
                - [ ] 审查该时间段内创建的所有新资源。

            **事件 2: [另一个事件标题]**
            - **严重等级**: ...
            - ... (结构与事件1相同)

            ---

            #### **总结与全局建议**

            1.  **立即遏制措施**: [列出需要最优先处理的1-3项行动。]
            2.  **证据保全建议**: [建议保留哪些日志和证据以供后续深入调查。]
            3.  **长期加固建议**:
                - 启用RAM用户MFA强制策略。
                - 配置ActionTrail日志投递到OSS并开启日志库的不可变性。
                - 使用云安全中心进行实时威胁检测。
                - 严格遵循最小权限原则。

            **免责声明**: 本分析基于提供的日志数据，是初步的威胁评估。建议由安全工程师进行最终确认和深入调查。
            """
            prompt = f"""      
                    {FUNCTION.strip()}
                   目标受众：
                   {AUDIENCE.strip()} 
                    {FUNCTION.strip()}    
                    请按以下思维步骤（Chain-of-Thought）逐步推理：
                    {STEPS.strip()}
                    输出必须严格遵循以下模板：
                    {TEMPLATE.strip()}
                    强制规则：
                    {self.mandatory_rules.strip()}
                    约束条件：
                    {self.constraints.strip()}
                                    """

            return PromptMessage(
                role="assistant",
                content=TextContent(
                    type="text",
                    text=prompt)
            )